{
  "name": "287trans",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && cross-env DATABASE_URL=file:./placeholder-build.db npm run prisma:generate --prefix server",
    "start": "npm run prisma:deploy --prefix server && npm run start --prefix server"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
